apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nodejs-ingress
  namespace: ivolve
spec:
  rules:
    - host: ivolvee.com
    #- host: ivolve.com // to test ingress in argocd
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nodejs-service
                port:
                  number: 80
